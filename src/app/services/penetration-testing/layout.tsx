import { penetrationTestingMetadata as metadata } from "./metadata";

export { metadata };

export default function PenetrationTestingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}