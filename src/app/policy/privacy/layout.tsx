import { privacyPolicyMetadata as metadata } from "./metadata";

export { metadata };

export default function PrivacyPolicyLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}