import { termsOfServiceMetadata as metadata } from "./metadata";

export { metadata };

export default function TermsOfServiceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}