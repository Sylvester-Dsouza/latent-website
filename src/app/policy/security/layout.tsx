import { securityPolicyMetadata as metadata } from "./metadata";

export { metadata };

export default function SecurityPolicyLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}