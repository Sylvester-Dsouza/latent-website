import { cookiePolicyMetadata as metadata } from "./metadata";

export { metadata };

export default function CookiePolicyLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}